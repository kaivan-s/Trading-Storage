# pyarrow is optional: the bhavcopy cache falls back to gzipped CSV without it.
# If you hit "_ARRAY_API not found", that is a NumPy 1.x/2.x binary mismatch --
# see the README. A clean virtualenv fixes it.
pandas>=2.0
numpy>=1.24
requests>=2.28
flask>=3.0
flask-cors>=4.0
gunicorn>=22.0
growwapi>=0.1
pyotp>=2.9
